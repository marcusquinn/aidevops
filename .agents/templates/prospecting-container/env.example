# The loopback-only host port. Do not expose publicly without the documented review.
PROSPECTING_PORT=8765
# Optional secure-store handle only; never put a credential value in this file.
AIDEVOPS_PROSPECTING_PROVIDER_SECRET_HANDLE=
